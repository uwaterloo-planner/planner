import React from "react"
import { Grid } from "@mui/material"

const Footer: React.FC = () => {
    return (
        <Grid container={true} sx={{height:50}} className='bg-blue-300 sticky bottom-0'>
        </Grid>)
}

export default Footer
